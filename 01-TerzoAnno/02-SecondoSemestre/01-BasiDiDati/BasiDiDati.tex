\documentclass[12pt, a4paper, oneside]{book}

% --- LINGUA E CODIFICA ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[italian]{babel}
\renewcommand{\familydefault}{\sfdefault} % Sans-serif moderno

\usepackage{mdframed}

% --- MARGINI E GEOMETRIA ---
\usepackage[a4paper, margin=2.5cm]{geometry}
\usepackage[export]{adjustbox}

% --- COLORI E LINK ---
\usepackage{xcolor}
\definecolor{azzurrino}{HTML}{0D47A1}
\definecolor{grigiochiaro}{HTML}{F5F5F5}
\definecolor{grigioscuro}{HTML}{424242}
\definecolor{keywordcolor}{RGB}{128, 0, 128} % Viola
\definecolor{backcolor}{RGB}{245, 245, 245}  % Grigio chiaro sfondo
%altra opzione: 1A237E



\usepackage[
    colorlinks=true,
    linkcolor=grigioscuro,
    citecolor=azzurrino,
    urlcolor=azzurrino
]{hyperref}

% --- TITOLI MODERNI ---
\usepackage{titlesec}

\titleformat{\chapter}[display]
  {\normalfont\Huge\bfseries\color{azzurrino}}
  {\filleft\Huge\thechapter}
  {0pt}
  {\titlerule[1pt]\vspace{1ex}\filright}
  [\vspace{1ex}\titlerule]

\titleformat{\section}
  {\normalfont\Large\bfseries\color{azzurrino}}{\thesection}{1em}{}

\titleformat{\subsection}
  {\normalfont\large\bfseries\color{grigioscuro}}{\thesubsection}{1em}{}

\setcounter{tocdepth}{4}
\setcounter{secnumdepth}{4}

\titleformat{\subsubsection}
  {\normalfont\large\bfseries\color{grigioscuro}}{\thesubsubsection}{1em}{}

% --- CAPTION COLORATE ---
\usepackage[font=small, labelfont={bf, color=azzurrino}]{caption}

% --- HEADER E FOOTER ---
\usepackage{fancyhdr}
\setlength{\headheight}{25pt}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\color{grigioscuro}\leftmark}
\fancyhead[R]{\color{azzurrino}\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{azzurrino}\leaders\hrule height \headrulewidth\hfill}}
\fancyfoot[C]{\color{grigioscuro}\small Basi di dati – \thepage}

% --- LISTE ED ENUMERAZIONI ---
\usepackage{enumitem}
\setlist{nosep, leftmargin=1.5em}

% --- GRAFICA E FIGURE ---
\usepackage{graphicx}
\usepackage{bookmark}
\usepackage{float}
\usepackage{wrapfig}
\usepackage[normalem]{ulem} % sottolineature
\usepackage{subcaption}
\usepackage{soul} % evidenziazione
\usepackage{parskip} % spaziatura tra paragrafi
\usepackage{tikz}

% --- Tcolorbox e Listings ---
\usepackage[most]{tcolorbox}
\tcbuselibrary{listings, skins, breakable, theorems} % theorems utile per ambienti come erexercise
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{textcomp}
\usepackage{listings}

% --- Tabelle ---
\usepackage{longtable}
\usepackage{multirow}
\usepackage{array}
\usepackage{tabularx}

% --- FRONTESPIZIO ---
\title{\Huge\bfseries Appunti del corso di \\[0.5ex]
\textcolor{azzurrino}{Basi di dati} \\[1ex]
\Large UniVR - Dipartimento di Informatica \\[1.5ex]
\Large secondo semestre - A.A. 2025/26}
\author{\bfseries \Large \textcolor{azzurrino}{Mattia Nicolis} \and \bfseries \Large \textcolor{azzurrino}{Matteo Drago}}
\date{}

\input{../../../ambienti.sty} %file apposito per definire nuovi comandi


% =================================
\begin{document}
    \frontmatter
    \maketitle
    \tableofcontents
    \thispagestyle{empty}
    \newpage

    %===========================
    \mainmatter


    \chapter{Tecnologie per le basi di dati}
    Argomenti visti nel Modulo di Teoria:
    \begin{itemize}
      \item Progettazione concettuale
      \item Progettazione logica
      \item Linguaggi di interrogazione (algebra relazionale e calcolo relazionale)
    \end{itemize}

    \section{Tecnologia dei DBMS}
    Un DBMS \textbf{(Database Management System)} o sistema di gestione di basi di dati è un sistema software in grado di gestire collezioni di dati che siano e \textbf{persistenti} assicurando la loro \textbf{affidabilità} e \textbf{privatezza}.

    Un DBMS in quanto sistema software deve essere efficiente ed efficace e deve inoltre estendere le funzionalità del file system.
    Esempio: Gestione in memoria secondaria collezioni di dati.


    \definizione{Basi di dati}{Una base di dati è una collezione di dati gestita da un DBMS.}

    \begin{figure}[H]
      \centering
      \includegraphics[width=0.9\textwidth]{images/schema.png} 
      \caption{Architettura su 3 livelli del DBMS.}
    \end{figure}

    Un DBMS basato sul modello relazionale è nella maggior parte dei casi anche un sistema transazionale: fornisce un meccanismo per la definizione ed esecuzione di transazioni.

    \begin{figure}[H]
      \centering
      \includegraphics[width=0.9\textwidth]{images/DBMS.png}
      \caption{Ssistema transizionale}
    \end{figure}

    In sintesi, durante il corso si affronteranno i seguenti argomenti:
    \begin{itemize}
      \item Transazioni: definizione e proprietà
      \item Architettura di un DBMS:
      \begin{itemize}
        \item Gestore delle interrogazioni: scrittura query in SQL
        \item Gestore delle interrogazioni: ottimizzazione delle interrogazioni
        \item Gestore dei metodi di accesso: indici primari e secondari, indici B+-tree e indici Hash
        \item Gestore del buffer: interazione tra memoria secondaria e memoria centrale
        \item Gestore della memoria secondaria: strutture fisiche per la memorizzazione dei dati in memoria secondari
        \item Gestore della concorrenza: gestione dell’esecuzione concorrente (VSR, CSR, locking a due fasi)
        \item Gestore dell’affidabilità: gestione dei guasti
        \end{itemize}
      \item Architettura di un sistema document-based
      
    \end{itemize}


    \subsection{Transazione}

    \definizione{Transazione}{È \textcolor{red}{un’unità di lavoro} svolta da un programma applicativo (che interagisce con una base di dati) per la quale si vogliono garantire proprietà di correttezza, robustezza e isolamento.}

    Le caratteristiche principali sono:
    \begin{itemize}
      \item Una transazione o va a buon fine e ha effetto sulla base di dati o abortisce e non ha nessun effetto sulla base di dati.
      \item \textcolor{red}{O tutto o niente!} Non sono ammesse esecuzioni parziali
    \end{itemize}


    Sintassi per definire una transazione in SQL:
     \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt] 
    \textcolor{purple}{<transazione> $\rightarrow$ begin transaction}\par
    \textcolor{purple}{\hspace{8em}<programma>}\par
    \textcolor{purple}{\hspace{8em}end transaction}\\
     \end{mdframed}

      \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt] 
   \textcolor{purple} {<programma> $\rightarrow$ $\langle$ <istruzione> | commit work | rollback work $\rangle$\\}
    \hspace{8em}\textcolor{purple}{\{ $\langle$ <istruzione> | commit work | rollback work $\rangle$\}}
      \end{mdframed}

    La transazione va a buon fine all’esecuzione di un commit work ma non ha alcun effetto se viene eseguito un rollback work.
    
    Una transazione è ben formata se:
    \begin{itemize}
      \item Inizia con un begin transaction.
      \item Termina con un end transaction.
      \item La sua esecuzione comporta il raggiungimento di un commit o di un rollback work e dopo il commit/rollback non si eseguono altri accessi alla base di dati.
    \end{itemize}

    Esempio di transazione ben formata:
     \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt] 
    \textcolor{cyan}{begin transaction}\\
    \textcolor{cyan}{\hspace{2em}update CONTO set saldo = saldo - 1200}\\
    \textcolor{cyan}{\hspace{4em}where filiale = '005' and numero = 15;}\\
    \textcolor{cyan}{\hspace{2em}update CONTO set saldo = saldo + 1200}\\
    \textcolor{cyan}{\hspace{4em}where filiale = '005' and numero = 205}\\
    \textcolor{cyan}{\hspace{2em}commit work;\\
    end transaction}
     \end{mdframed}


    \vario{Proprietà delle transazioni}{Una transazione ha quattro proprietà:
    \begin{itemize}
    \item Atomicità
    \item Consistenza
    \item Isolamento
    \item Persistenza
    \end{itemize}
    }

    \subsection{Atomicità}
    Una transazione è una unità di esecuzione \textbf{indivisibile}. O viene eseguita completamente o non viene eseguita affatto.\par
    Implicazioni:
    \begin{itemize}
      \item Se una transazione viene \textbf{interrotta prima} del commit, il lavoro fin qui eseguito dalla transazione deve essere disfatto ripristinando la situazione in cui si trovava la base di dati prima dell’inizio della transazione. 
      \item Se una transazione viene \textbf{interrotta dopo} l’esecuzione del commit (commit eseguito con successo), il sistema deve assicurare che la transazione abbia effetto sulla base di dati.

    \end{itemize}
    
    \subsubsection{Consistenza}
  L’esecuzione di una transazione non deve violare i \textbf{vincoli di integrità}.\par
  Implicazioni:
    \begin{description}
      \item[Verifica immediata:]
      \begin{itemize}
        \item Fatta nel corso della transazione
        \item Viene abortita solo l’ultima operazione e il sistema restituisce all’applicazione una segnalazione d’errore
        \item L’applicazione può quindi reagire alla violazione.
      \end{itemize}
      \item[Verifica differita:] 
      \begin{itemize}
        \item Al commit se un vincolo di integrità viene violato la transazione viene abortita senza possibilità da parte dell’applicazione di reagire alla violazione
      \end{itemize}
    \end{description}
    

    

    \subsubsection{Isolamento}
    L’esecuzione di una transazione deve essere \textbf{indipendente} dalla contemporanea esecuzione di altre transazioni.

    Implicazioni:
    \begin{itemize}
      \item Il rollback di una transazione non deve creare rollback a catena di altre transazioni che si trovano in esecuzione contemporaneamente.
      \item Il sistema deve regolare l’esecuzione concorrente con meccanismi di controllo dell’accesso alle risorse
    \end{itemize}


    \subsubsection{Persistenza}
    L’effetto di una transazione che ha eseguito il commit non deve andare perso.
    Implicazioni:
    \begin{itemize}
      \item Il sistema deve essere in grado, in caso di guasto, di garantire gli effetti delle transazioni che al momento del guasto avevano già eseguito un commit.
    \end{itemize}

    \newpage

    \section{Architettura di un DBMS}
    
    L’architettura mostra i moduli principali che possiamo individuare nei DBMS attuali, considerando le diverse funzionalità che il DBMS svolge durante l’esecuzione delle transazioni. Per ogni modulo dell’architettura presentiamo le funzionalità che esso svolge e alcune delle tecniche che applicativo

    \begin{figure}[H]
      \centering
      \includegraphics[width=0.9\textwidth]{images/ArchitetturaDBMS.png} 
      \caption{Architettura di riferimento di un DBMS.}
    \end{figure}

    \begin{figure}[H]
      \centering
      \includegraphics[width=0.9\textwidth]{images/Architettura2.png} 
      \caption{Architettura di riferimento di un DBMS.}
    \end{figure}

    
    \begin{figure}[H]
      \centering
      \includegraphics[width=0.9\textwidth]{images/passaggio1.png} 

    \end{figure}

    
    \begin{figure}[H]
      \centering
      \includegraphics[width=0.9\textwidth]{images/passaggio2.png} 
    \end{figure}

    
    \begin{figure}[H]
      \centering
      \includegraphics[width=0.9\textwidth]{images/passaggio3.png} 
    \end{figure}

    
    \begin{figure}[H]
      \centering
      \includegraphics[width=0.9\textwidth]{images/passaggio4.png} 
    \end{figure}

    Quali modulo contribuiscono a garantire le proprietà delle transizioni?
    \begin{description}
      \item[Gestore dei metodi d'accesso:] Consistenza
      \item[Gestore dell'esecuzione concorrente:]Atomicità e Isolamento
      \item[Gestore dell'affidabilità:] Atomicità e Persistenza  
    \end{description}



    \chapter{SQL}
    Il \textcolor{azzurrino}{\textbf{termine SQL}} in origine era l'acronimo di \textbf{S}tructured \textbf{Q}uery \textbf{L}enguagge, ma ad oggi è diventato un nome proprio.

    SQL è un linguaggio che permette di:
    \begin{itemize}[label=$\triangleright$]
      \item \textbf{definire delle strutture dati e i vincoli di integrità}: \textcolor{azzurrino}{\textbf{D}}ata \textcolor{azzurrino}{\textbf{D}}efinition \textcolor{azzurrino}{\textbf{L}}enguagge
      \medskip
      \item \textbf{manipolare i dati} (inserimento, aggiornamento e cancellazione): \textcolor{azzurrino}{\textbf{D}}ata \textcolor{azzurrino}{\textbf{M}}anipilation \textcolor{azzurrino}{\textbf{L}}enguagge
      \smallskip
      \item \textbf{interrogare}: \textcolor{azzurrino}{\textbf{D}}ata \textcolor{azzurrino}{\textbf{Q}}uery \textcolor{azzurrino}{\textbf{L}}enguagge
    \end{itemize}

    Esistono diverse versioni:

    \begin{center}
      \begin{longtable}{|c|c|l|}
      \hline
      \textbf{Nome informale} & \textbf{Nome ufficiale} & \multicolumn{1}{c|}{\textbf{Caratteristiche}} \\ \hline
      \endfirsthead
      \hline
      \textbf{Nome informale} & \textbf{Nome ufficiale} & \multicolumn{1}{c|}{\textbf{Caratteristiche}} \\ \hline
      \endhead

      \multirow{2}{*}{SQL base} & SQL-86 & Costrutti base \\ \cline{2-3} 
                                & SQL-89 & Integrità referenziale \\ \hline
      SQL-2                     & SQL-92 & \begin{tabular}[c]{@{}l@{}}Modello relazionale\\ Vari costrutti nuovi\\ 3 livelli: entry, intermediate, full\end{tabular} \\ \hline
      \multirow{10}{*}{SQL-3}   & SQL:1999 & \begin{tabular}[c]{@{}l@{}}Modello relazionale a oggetti\\ Organizzato in diverse parti\\ Trigger, funzioni esterne, ...\end{tabular} \\ \cline{2-3} 
                                & SQL:2003 & \begin{tabular}[c]{@{}l@{}}Estensioni del modello a oggetti\\ Eliminazione di costrutti non usati\\ Nuove parti: SQL/JRT, SQL/XML, ...\end{tabular} \\ \cline{2-3} 
                                & SQL:2006 & Estensione della parte XML \\ \cline{2-3} 
                                & SQL:2008 & Lievi aggiunte (per esempio, \texttt{trigger instead of}) \\ \cline{2-3} 
                                & SQL:2011 & Ricco supporto per dati temporali \\ \cline{2-3} 
                                & SQL:2016 & Gestione del formato JSON \\ \hline
      \end{longtable}
    \end{center}

    La parte di SQL dedicata alle dichiarazioni fa parte dei DML.\\[0.2ex]
    Le dichiarazioni vengono espresse in modo dichiarativo: specifica l'obiettivo e non la procedura per ottenerlo (algebra relazionale).

    \section{Istruzione SELECT}

    \begin{lstlisting}
    SELECT [ DISTINCT ]
    [ * | expression [[ AS ] output_name ] [, ...] ]
    [ FROM from_item [, ...] ]
    [ WHERE condition ]
    [ GROUP BY grouping_element [, ...] ]
    [ HAVING condition [, ...] ]
    [ { UNION | INTERSECT | EXCEPT } [ DISTINCT ] other_select ]
    [ ORDER BY expression [ ASC | DESC | USING operator ]]
    \end{lstlisting}

    \subsection{Struttura}

    \begin{description}
      \item[expression:] è un’espressione che determina un attributo.
      \item[from\_item:] è un’espressione che determina una sorgente per gli attributi.
      \item[condition:]è un’espressione booleana per selezionare i valori degli attributi.
      \item[grouping\_element:] è un’espressione per poter eseguire operazioni su più valori di un attributo e considerare il risultato.
    \end{description}

    \begin{flushleft}
        \textcolor{cyan}{SELECT} attributo\\
        \textcolor{cyan}{FROM} tabella\\
        {[} \textcolor{cyan}{WHERE} Condizione {]}\\
    \end{flushleft}

    \begin{itemize}
      \item SELECT, FROM e WHERE sono chiamate clausole
      \item Estrae dal prodotto cartesiano delle tabelle elencate nella clausola \textcolor{cyan}{FROM}, le righe che soddisfano la condizione specificata nella clausola WHERE.
      \begin{itemize}
        \item Il risultato dell‘interrogazione è una tabella con una riga per ogni riga prodotta dalla clausola \textcolor{cyan}{FROM} ed eventualmente filtrata dalla clausola \textcolor{cyan}{WHERE} (se esiste), le cui colonne dipendono dalla lista degli attributi nella clausola SELECT.
      \end{itemize}
    \end{itemize}

    \vspace{2em}

    \begin{lstlisting}
    SELECT [ DISTINCT ]
    [ * | expression [[ AS ] output_name ] [, ...] ]
    \end{lstlisting}

    \begin{itemize}
      \item \textcolor{purple}{*} è un’abbreviazione per indicare tutti gli attributi delle tabelle.
      \item \textcolor{purple}{expression} è un’espressione che coinvolge gli attributi della tabella (può anche essere semplicemente il nome di un attributo).
      \item \textcolor{purple}{output\_name} è il nome assegnato all’attributo che conterrà il risultato della valutazione dell’espressione expression nella relazione risultato.
      \item \textcolor{purple}{DISTINCT}: se presente richiede l’eliminazione delle tuple duplicate.

    \end{itemize}

    \subsection{Interrogazione: Clausola SELECT}

    \begin{figure}[H]
      \centering
      \includegraphics[width=1\textwidth]{images/Q1.png} 
  
    \end{figure}


    Un'alternativa a Q1 è questa:
     \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt] 
    \begin{flushleft}
        SELECT Stipendio \textcolor{cyan}{as} Salario $\leftarrow$ \textcolor{purple}{alias}\\ 
        FROM Impiegato\\
        WHERE Cognome = 'Rossi'\\
    \end{flushleft}
  \end{mdframed}

    Restituisce lo stesso risultato della query precedente, ma \textbf{ridenomina} (\underline{temporaneamente}) la colonna output Stipendio

    \subsection{Interrogazione: Clausola SELECT ALL}
    
     \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt] 
    \begin{flushleft}
        SELECT Stipendio \textcolor{purple}{*} \hspace{4em} $\leftarrow$ \textcolor{purple}{Seleziona tutti gli attributi della tabella}\\
        FROM Impiegato\\
        WHERE Cognome = 'Rossi'\\
    \end{flushleft}
  \end{mdframed}


    L'\textbf{asterisco} permette di non elencare il nome di tutte le colonne di una tabella (simile alla proiezione)

    \begin{figure}[H]
      \centering
      \includegraphics[width=0.9\textwidth]{images/SelectAll.png}
    \end{figure}


    \subsection{Interrogazione: Clasuola FROM}


    \begin{lstlisting}
    FROM from_item [, ...]
    \end{lstlisting}


    dove \textcolor{purple}{from\_item} può essere UNA delle seguenti clausole (versione semplificata):
    \begin{enumerate}
      \item table\_name [[\textcolor{purple}{AS}] alias [(column\_alias [, ...])]
      \begin{itemize}
        \item Se sono presenti due o più tabelle, si esegue il prodotto cartesiano tra tutte le tabelle. Se ci sono attributi con lo stesso nome su tabelle diverse, nella SELECT questi attributi sono identificati anteponendo il nome della tabella e un ’.’: nomeTabella.nomeAttributo
      \end{itemize}
      \item (other\_select) [\textcolor{purple}{AS}] nomeRisultato [(column\_alias [,...])]
      \begin{itemize}
        \item è una \textcolor{purple}{SELECT} innestata. Il risultato della \textcolor{purple}{SELECT} interna è lo schema con nome nomeRisultato su cui fare la \textcolor{purple}{SELECT} corrente.
      \end{itemize}
      \item from\_item [\textcolor{purple}{NATURAL}] join\_type from\_item [\textcolor{purple}{ON} condition]
      \begin{itemize}
        \item è la clausola JOIN.
        \item Metodo più sofisticato di 1) che permette di selezionare un sottoinsieme del prodotto cartesiano di due o più tabelle. join\_type specifica il tipo di join. I tipi principali sono riassunti nelle slide seguenti.

      \end{itemize}
    \end{enumerate}

    
    \begin{figure}[H]
      \centering
      \includegraphics[width=1\textwidth]{images/Q2.png}
    \end{figure}

    In alternatica nella voce FROM Impiegato, Dipartimento potevo utilizzare un alias.

    
    \subsection{Interrogazione: Clausola WHERE}

    \begin{lstlisting}
    WHERE condition
    \end{lstlisting}


    \begin{itemize}
      \item La clausola \textcolor{purple}{WHERE} ammette come argomento un’espressione booleana costruita combinando predicati semplici con gli operatori \textcolor{purple}{AND}, \textcolor{purple}{OR} e \textcolor{purple}{NOT}
      \begin{itemize}
        \item AND: seleziona le righe in cui tutti i predicati sono veri
        \item OR: seleziona le righe in cui almeno un predicato è vero
        \item NOT: seleziona le righe in cui il predicato è falso
      \end{itemize}
      \item Ciascun predicato semplice usa gli operatori \textcolor{purple}{=} , \textcolor{purple}{<>}, \textcolor{purple}{<}, \textcolor{purple}{>}, \textcolor{purple}{<=} e \textcolor{purple}{>=} per confrontare un’espressione costruita a partire dal valore di un attributo con un valore costante, oppure un’altra espressione.
      \item Per esprimere la precedenza degli operatori AND e OR si fa uso della parentesi.
    \end{itemize}

    \begin{figure}[H]
      \centering
      \includegraphics[width=1\textwidth]{images/Q3.png}
    \end{figure}

    \begin{figure}[H]
      \centering
      \includegraphics[width=1\textwidth]{images/Q4.png}
    \end{figure}


    \begin{figure}[H]
      \centering
      \includegraphics[width=1\textwidth]{images/Q5.png}
    \end{figure}

    \newpage

    \section{Clausola WHERE}
    \subsection{Operatore LIKE}
    \begin{itemize}
      \item Per il confronto tra stringhe, SQL mette a disposizione anche l’operatore \textcolor{purple}{LIKE} che consente verificare se una stringa rispetta un certo “pattern” (oppure \textcolor{purple}{ILIKE} per \textbf{case insentive}).
      \item Due caratteri speciali per il confronto “\_” e “\%”:
      \begin{itemize}
        \item “\_”: confronto con un carattere arbitrario
        \item “\%”: confronto con una stringa di lunghezza arbitraria (anche 0) di caratteri arbitrari
      \end{itemize}
      \item ‘\_o\%i’ = stringa che ha una o in seconda posizione, seguita da una sequenza qualsiasi di caratteri (anche vuota) e che termina con una i.
    \end{itemize}

    \begin{figure}[H]
      \centering
      \includegraphics[width=1\textwidth]{images/Q6.png}
    \end{figure}


    \subsection{Operatore SIMILAR TO}

    \begin{itemize}
      \item  L’operatore \textcolor{purple}{SIMILAR TO} è un \textcolor{purple}{LIKE} più espressivo che accetta, come pattern, un sottoinsieme delle espressioni regolari POSIX (versione SQL).
      \item Esempi di componenti di espressioni regolari:
      \begin{itemize}
        \item \_ = 1 carattere qualsiasi.
        \item \% = 0 o più caratteri qualsiasi.
        \item * = ripetizione del precedente match 0 o più volte.
        \item + = ripetizione del precedente match UNA o più volte.
        \item {n,m} = ripetizione del precedente match almeno n e non più di m volte.
        \item [...] = ... è un elenco di caratteri ammissibili
      \end{itemize}
    \end{itemize}

      \begin{figure}[H]
      \centering
      \includegraphics[width=1\textwidth]{images/EsempioSimilarTo.png}
    \end{figure}


    \subsection{Operatore BETWEEN}

     \begin{figure}[H]
      \centering
      \includegraphics[width=1\textwidth]{images/EsempioBetween.png}
    \end{figure}


    \subsection{Operatore IN}
    Nella clausola \textcolor{purple}{WHERE} può apparire l’operatore {[}\textcolor{purple}{NOT}{]} \textcolor{purple}{IN} per testare l’appartenenza di un valore ad un insieme.

    \begin{lstlisting}
    WHERE attributo [NOT] IN (valore, [, ...]);
    \end{lstlisting}

    
    \begin{figure}[H]
      \centering
      \includegraphics[width=1\textwidth]{images/EsempioIn.png}
    \end{figure}


    \subsection{Operatore IS NULL}
    \begin{itemize}
      \item Per verificare se un attributo ha un valore nullo o meno si utilizza il predicato \textcolor{purple}{IS NULL}.
      \item \textcolor{purple}{Attributo IS NULL}: ritorna true se l’attributo ha valore nullo.
      \item \textcolor{purple}{IS NOT NULL}: è la negazione, ritorna true se l’attributo ha valore diverso da nullo.
    \end{itemize}

     \begin{lstlisting}
    WHERE attributo IS [NOT] IN NULL;
    \end{lstlisting}

     SQL-2 implementa la logica a 3 valori:
    \begin{itemize}
      \item Un predicato semplice restituisce il valore NULL quando uno qualsiasi dei termini del predicato ha valore nullo.
      \item Il predicato IS NULL è una eccezione, restituendo sempre il valore vero oppure false, mai il valore NULL.
    \end{itemize}

    \begin{figure}[H]
      \centering
      \includegraphics[width=1\textwidth]{images/EsempioIsNull.png}
    \end{figure}



    \section{SQL e ALGEBRA RELAZIONALE}


    Cerchiamo ora di capire la relazione con le corrispondenti interrogazioni procedurali in algebra relazionale.

    Scrivere questa espressione in SQL: 
     \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]      
    \begin{flushleft}
        SELECT $T_1$.attributo$_{1l}$,$\dots$, T$_h$.attributo$_{hm}$\\
        FROM Tabella$_{1}$ as T$_{1}$,$\dots$, Tabella$_{h}$ as T$_{h}$\\
        WHERE Condizione
    \end{flushleft}
  \end{mdframed}

    Corrisponde a fare questa operazione in algebra relazionale:

    \begin{figure}[H]
      \includegraphics[width=0.7\textwidth]{images/SqlRelazionale1.png}
    \end{figure}

    {\textbf{\large Esempi più concreti}}

    Prendiamo queste tabelle come riferimento iniziale:
    \begin{figure}[H]
      \includegraphics[width=1\textwidth]{images/TabelleEsempio.png}
      \caption{Tabelle di riferimento}
    \end{figure}

    \subsection{Selezione e proiezione}
    \underline{Nome e reddito delle persone con meno di trenta anni}

    \textbf{Algebra relazionale}: $\pi_{\text{Nome, Reddito}}(\sigma_{\text{Età<30}}(\text{Persone}))$ 

     \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]      
    \noindent \textbf{SQL:} \quad
    \begin{tabular}[t]{@{}l@{}}
    SELECT Nome, Reddito\\
    FROM Persone\\
    WHERE età < 30
    \end{tabular}
  \end{mdframed}

    \vspace{2em}

    {\large \textbf{SELECT: alias di tabella o di attributo}}

    \noindent
\begin{minipage}[t]{0.39\textwidth}
   \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]      
    \textbf{Con ALIAS di Tabella}
    \begin{flushleft}
        SELECT \textcolor{cyan}{p}.nome, \textcolor{cyan}{p}.reddito\\
        FROM persone \textcolor{cyan}{as p}\\
        WHERE \textcolor{cyan}{p}.Età < 30
    \end{flushleft}
  \end{mdframed}
\end{minipage}
\hfill % Questo comando inserisce uno spazio flessibile che spinge le due minipage ai lati opposti
\begin{minipage}[t]{0.60\textwidth}
   \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]      
    \textbf{Con ALIAS di attributo}
    \begin{flushleft}
      SELECT \textcolor{cyan}{p}.nome as \textcolor{black!40!green}{nomePersona},
      \textcolor{cyan}{p}.reddito as \textcolor{black!40!green}{redditoPersona}\\
      FROM persone \textcolor{cyan}{as p}\\
      WHERE \textcolor{cyan}{p}.Età < ’30‘
    \end{flushleft}
  \end{mdframed}
\end{minipage}


    \subsection{SELECT: Senza proiezione}
    \underline{Nome, reddito ed età delle persone con meno di trenta anni}

    \textbf{Algebra relazionale}: $\sigma_{\text{Età<30}}(\text{Persone})$

     \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]      
    \noindent \textbf{SQL:} \quad
    \begin{tabular}[t]{@{}l@{}}
    SELECT *\\
    FROM Persone\\
    WHERE età < 30
    \end{tabular}
  \end{mdframed}


    \subsection{Proiezione: gestione dei duplicati in SQL}

    Una significativa differenza tra SQL e algebra relazionale è la gestione dei duplicati.
    L’operazione di eliminazione delle righe doppie (ad esempio al seguito di una selezione con where) è molto costosa, quindi di default SQL mantiene le righe duplicate.
    
    Il costrutto \textcolor{purple}{DISTINCT} dopo la parola chiave SELECT permette di eliminare i duplicati. Il costrutto \textcolor{purple}{ALL} permette invece di mantenerli, ma questa è già la scelta di default.


    {\textbf{\large Vediamone un esempio}}
    \begin{figure}[H]
      \includegraphics[width=0.8 \textwidth]{images/Impiegati.png}
    \end{figure}
    \underline{Trovare cognome e filiale di tutti gli impiegati}

    \textbf{Algebra relazionale}: $\pi_{\text{Cognome,Filiale}}\text{(Impiegati)}$\\
    Output:\{(Neri, Napoli), (Rossi, Roma)\}



    \noindent
\begin{minipage}[c]{0.45\textwidth}
  \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]      
    \textbf{SQL}
    \begin{flushleft}
    SELECT Cognome, Filiale\\
    FROM Impiegati\\
    \end{flushleft}
        \end{mdframed}
    \hfill
\end{minipage}
\begin{minipage}[c]{0.45\textwidth}
    \centering
    % Inserisci qui il nome del tuo file immagine (es. schema.png)
    \includegraphics[width=\textwidth]{images/Impiegati2.png}
\end{minipage}


  \noindent
\begin{minipage}[c]{0.55\textwidth}
  \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]      
    \textbf{SQL}
    \begin{flushleft}
    SELECT DISTINCT Cognome, Filiale\\
    FROM Impiegati\\
    \end{flushleft}
  \end{mdframed}
  
\end{minipage}
\hfill
\begin{minipage}[c]{0.44\textwidth}
    \centering
    % Inserisci qui il nome del tuo file immagine (es. schema.png)
    \includegraphics[width=\textwidth]{images/Impiegati3.png}
\end{minipage}


\vspace{2em}

  \begin{figure}[H]
    \centering
    % Inserisci qui il nome del tuo file immagine (es. schema.png)
    \includegraphics[width=\textwidth]{images/Q7.png}
  \end{figure}


\subsection{SELECT: Selezione, Proiezione, Join}
L’istruzione SELECT, con una sola relazione nella clausola FROM permette di realizzare: \textcolor{black!40!green}{selezioni}, \textcolor{black!40!green}{proiezioni}, \textcolor{black!40!green}{ridenominazioni}.\\
Invece con più relazioni nella clausola FROM si realizzano join (e prodotti
cartesiani)


    \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]
  \begin{flushleft}
SELECT \hspace{3em} $\pi$Proiezione + $\rho$Ridenominazione (con AS)\\
FROM \hspace{4em} $\bowtie$Join(prodotto cartesiano)\\
WHERE \hspace{3em} $\sigma$Selezione + $\bowtie$ Join(condizion)\\       
    \end{flushleft}
  \end{mdframed}

    \subsection{Interrogazioni con JOIN}
In SQL il join si può definire in due modi diversi
\begin{itemize}
  \item Il primo non richiede l’uso di nuova sintassi, e specifica il legame tra le tabelle coinvolte a livello della clausola WHERE.
  \item Il secondo usa una sintassi riservata a livello di clausola FROM (useremo questo). 
\end{itemize}

 La clausola \textcolor{purple}{FROM} ammette come argomento:
  \begin{center}
    table\_name [[\textcolor{purple}{AS}] name]] [, ...] \\
  \end{center}
 Si è visto che se sono presenti due o più nomi di tabelle, si esegue il prodotto cartesiano tra tutte le tabelle e lo schema del risultato
può contenere tutti gli attributi del prodotto cartesiano. \\
Il prodotto cartesiano di due o più tabelle è un \textcolor{cyan}{CROSS JOIN}. \\
A partire da SQL-2, esistono altri tipi di JOIN (join\_type): \textcolor{cyan}{INNER JOIN}, \textcolor{cyan}{LEFT [OUTER] JOIN}, \textcolor{cyan}{RIGHT [OUTER] JOIN} e \textcolor{cyan}{FULL [OUTER] JOIN}.

  \qquad

\textbf{Sintassi generale}
\begin{lstlisting}
    table_name [NATURAL] join_type table_name [ ON join_condition [, ...]].
    \end{lstlisting}

dove \textcolor{cyan}{join\_condition} è un’espressione booleana che seleziona le tuple del join da aggiungere al risultato. Le tuple selezionate possono essere poi filtrate con la condizione della clausola \textcolor{purple}{WHERE} 



\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/Join.png}
\end{figure}

  \subsubsection{Interrogazioni con INNER JOIN}

    \textbf{Sintassi generale}
  \begin{lstlisting}
    table1 [ INNER ] JOIN table2 ON join_condition [, ...]
    \end{lstlisting}



  Rappresenta il tradizionale $\theta$ join dell’algebra relazionale. Combina ciascuna riga r1 di table1 con ciascuna riga di table2 che soddisfa la condizione della clausola \textcolor{purple}{ON}





   \noindent
\begin{minipage}[c]{0.45\textwidth}
  \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]
    \small
    \textbf{SQL}
    \begin{flushleft}
    \textcolor{purple}{SELECT} I.cognome , R.nomeRep, R.sede\\
    \textcolor{purple}{FROM} Impiegato \textcolor{purple}{AS} I\\
     \quad \textcolor{purple}{INNER JOIN} Reparto  \textcolor{purple}{AS} R\\
    \quad \textcolor{purple}{ON} I.nomerep = R.nomerep;

    \end{flushleft}
    \end{mdframed}

\end{minipage}
\hfill
\begin{minipage}[c]{0.45\textwidth}
    \centering
    % Inserisci qui il nome del tuo file immagine (es. schema.png)
    \includegraphics[width=\textwidth]{images/tabellina.png}
\end{minipage}


\subsubsection{Interrogazioni con LEFT OUTER JOIN}



  \textbf{Sintassi generale}
  \begin{lstlisting}
    table1 LEFT [ OUTER ] JOIN table2 ON join_condition [, ...].
    \end{lstlisting}


    Si esegue un \textcolor{purple}{INNER JOIN}. Poi, per ciascuna riga r1 di table1 che non soddisfa la condizione con qualsiasi riga di table2, si aggiunge una riga al risultato con i valori di r1 e assegnando \textcolor{purple}{NULL} agli altri attributi..



    \vspace{3em}
  
\noindent
\begin{minipage}[c]{0.55\textwidth}
    % --- INIZIO RIQUADRO ---
    \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]
        \textbf{SQL}
        \begin{flushleft}
          \small 
          \textcolor{purple}{INSERT INTO} Reparto (nomerep, sede, telefono) \textcolor{purple}{VALUES} ('Finanza', 'Padova', '02 8028888');\\
          \textcolor{purple}{SELECT} R.nomeRep, I.cognome\\
          \textcolor{purple}{FROM} Reparto \textcolor{purple}{AS} R\\
          \quad \textcolor{purple}{LEFT OUTER JOIN} Impiegato \textcolor{purple}{AS} I\\ 
          \quad \textcolor{purple}{ON} I.nomerep = R.nomerep;
        \end{flushleft}
    \end{mdframed}
    % --- FINE RIQUADRO ---
\end{minipage}
\hfill
\begin{minipage}[c]{0.40\textwidth} 
    \centering
    \includegraphics[width=\textwidth]{images/tabellina2.png}
\end{minipage}




  \vario{Nota}{\underline{Il \textcolor{purple}{LEFT} [\textcolor{purple}{OUTER}] \textcolor{purple}{JOIN} non è simmetrico!}\\
  Con le medisime tabelle si possono ottenere risultati diversi invertendo l’ordine delle tabelle nel join!

  \noindent
\begin{minipage}[c]{0.55\textwidth}
    % --- INIZIO RIQUADRO ---
    \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]
        \textbf{SQL}
        \begin{flushleft}
          \small 
          \textcolor{purple}{SELECT} I.cognome, R.nomeRep\\
\textcolor{purple}{FROM} Impiegato I \textcolor{purple}{LEFT OUTER JOIN} RepartoR\\
\textcolor{purple}{ON} I.nomerep = R.nomerep 
        \end{flushleft}
    \end{mdframed}
    % --- FINE RIQUADRO ---
\end{minipage}
\hfill
\begin{minipage}[c]{0.40\textwidth} 
    \centering
    \includegraphics[width=\textwidth]{images/tabellina3.png}
\end{minipage}

}


\subsubsection{Interrogazioni con RIGHT OUTER JOIN}


\textbf{Sintassi generale}
 \begin{lstlisting}
    table1 RIGHT [ OUTER ] JOIN table2 ON join_condition [, ...].
    \end{lstlisting}




    \noindent
\begin{minipage}[c]{0.55\textwidth}
    % --- INIZIO RIQUADRO ---
    \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]
        \textbf{SQL}
        \begin{flushleft}
          \small 
          \textcolor{purple}{SELECT} I.cognome , R.nomeRep\\
\textcolor{purple}{FROM} Impiegato I \textcolor{purple}{RIGHT OUTER JOIN} RepartoR\\
\textcolor{purple}{ON} I. nomerep = R. nomerep
        \end{flushleft}
    \end{mdframed}
    % --- FINE RIQUADRO ---
\end{minipage}
\hfill
\begin{minipage}[c]{0.40\textwidth} 
    \centering
    \includegraphics[width=\textwidth]{images/tabellina2.png}
\end{minipage}

\subsubsection{Interrogazioni con FULL OUTER JOIN
}
\textbf{Sintassi generale}
 \begin{lstlisting}
   table1 FULL [ OUTER ] JOIN table2 ON join_condition [, ...].

    \end{lstlisting}

 È equivalente a:
    \begin{itemize}
      \item INNER JOIN + LEFT OUTER JOIN + RIGHT OUTER JOIN.
      \item \textcolor{black!40!red}{Non è equivalente a CROSS JOIN!}
    \end{itemize}

    \vspace{2em}

    \textbf{\large Esempio}\\
    \underline{ Nome, Cognome di tutti gli impiegati e città del Dipartimento in cui lavora}


    \begin{figure}[H]
      \centering
      \includegraphics[width=\textwidth]{images/esempioJoin.png}
    \end{figure}

Il join si può spostare nella clausola from, a livello delle tabelle (nello svolgere gli esercizi, useremo questa sintassi dedicata).

\newpage

  \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]      
    \noindent \textbf{SQL:} \quad
    \begin{tabular}[t]{@{}l@{}}
    SELECT ListaAttributi\\
    FROM  Tabella1 \textcolor{purple}{JOIN} Tabella2 \textcolor{purple}{ON} ( CondizioneDiJoin )\\
    {[}WHERE AltraCondizione {]} 
    \end{tabular}

  \end{mdframed}

  \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]      
    \noindent \textbf{SQL:} \quad
    \begin{tabular}[t]{@{}l@{}}
   SELECT i.Nome, d.Città\\
FROM Impiegato AS i JOIN Dipartimento as d ON ( i.Dipart = \\d.Nome ) 
    \end{tabular}
  \end{mdframed}


  \begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/Q8.png}
  \end{figure}


  \subsection{Uso di variabili(alias)}
  SQL permette di associare un nome alternativo (alias) alle tabelle che compaiono nella clausola FROM. Il nome viene usato per far riferimento alla tabella nel contesto dell’interrogazione Motivi:
  \begin{itemize}
    \item Far riferimento alla tabella in modo compatto.
    \item Fare accesso più volte alla stessa tabella
  \end{itemize}

  \begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/Q9.png}
    \caption{Questa interrogazione confronta ciascuna riga di Impiegato con tutte le righe di Impiegato associate al dipartimento Produzione.}
  \end{figure}



  \textbf{\large Funzionamento}
   Si può immaginare che al momento della definizione dell’alias vengano create due copie della tabella Impiegato, ciascuna associata ad una delle due variabili i1 e i2.
   
   \noindent
\begin{minipage}[c]{0.50\textwidth}
    \begin{itemize}
        \item Le due copie contengono tutte le righe di Impiegato.
        \item Sulla copia i2 vengono selezionate solo le righe del dipartimento `Produzione'.
    \end{itemize}
\end{minipage}
\hfill
\begin{minipage}[c]{0.45\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/Alias.png}
\end{minipage}



  \subsection{Ordinamento del risultato}

   Come abbiamo visto, una relazione è un insieme di tuple non ordinato. Nella realtà, però, sorge spesso il bisogno di costruire un ordine sulle righe.
    SQL permette di specificare un ordinamento (ascendente o discendente)
sulle righe dell‘output grazie alla clausola ORDER BY che chiude
l‘interrogazione.

\textbf{Sintassi generale}

\begin{lstlisting}
    ORDER BY attributo [{ ASC | DESC }] [, ... ];
    \end{lstlisting}
 Le righe vengono ordinate per il primo attributo nell’elenco, a parità di valore
del primo attributo si usa il secondo, e così via in sequenza.


    \begin{figure}[H]
      \centering
      \includegraphics[width=\textwidth]{images/Q10.png}
    \end{figure}


 \begin{figure}[H]
      \centering
      \includegraphics[width=\textwidth]{images/Q11.png}
    \end{figure}

    \subsection{Operazioni Insiemistiche}
    SQL mette a disposizione degli operatori insiemistci

  \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]
     \begin{flushleft}
        SELECT
{ \textcolor{purple}{UNION} | \textcolor{purple}{INTERSECT}| \textcolor{purple}{EXCEPT}\\
{[}\textcolor{purple}{ALL}] SELECT }
    \end{flushleft}
  \end{mdframed}
    
    Gli operatori insiemistici, al contrario del resto del linguaggio, assumono come default di eseguire un’eliminazione dei duplicati, a
meno che si usi \textcolor{purple}{ALL}.\par
Sono sufficienti domini compatibili ed ugual numero di attributi.
    \begin{itemize}
      \item \textcolor{cyan}{Ordine posizionale}: la corrispondenza degli attributi si basa sulla posizione
      \item Se i nomi degli attributi sono diversi di solito il sistema usa il nome del primo operando
    \end{itemize}



\subsubsection{Unione}

\begin{figure}[H]
      \centering
      \includegraphics[width=\textwidth]{images/Q12.png}
    \end{figure}

    \begin{figure}[H]
      \centering
      \includegraphics[width=\textwidth]{images/Q13.png}
    \end{figure}

\subsubsection{Intersezione}
\begin{figure}[H]
      \centering
      \includegraphics[width=\textwidth]{images/Q14.png}
    \end{figure}

\subsubsection{Differenza}

    \begin{figure}[H]
      \centering
      \includegraphics[width=\textwidth]{images/Q15.png}
    \end{figure}


    \subsection{Operatori aggregati
    }

    Una delle più importanti estensioni di SQL rispetto all’algebra relazionale sono gli operatori: \textcolor{purple}{COUNT, SUM, MAX, MIN e AVG} \\
    In algebra relazionale tutte le condizioni specificate vengono valutate su una tupla alla volta.\par
    Gli operatori di aggregazione permettono di valutare proprietà che
dipendono da insiemi di tuple.
\begin{itemize}
  \item Prima viene eseguita l’interrogazione considerando solo le clausole
\textcolor{purple}{FROM} e \textcolor{purple}{WHERE}, quindi l’operatore di aggregazione viene applicato alla tabella contenente il risultato

\end{itemize}

\subsubsection{COUNT}

L'operatore COUNT conta il numero di righe di una tabella o il numero di valori di uno o più attributi: 
  \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]
\begin{flushleft}
    \textcolor{purple}{COUNT} ( $\langle$\textcolor{cyan}{*} | [\textcolor{cyan}{DISTINCT} | \textcolor{cyan}{ALL}] ListaAttributi$\rangle$ )
\end{flushleft}
\end{mdframed}

\begin{itemize}
    \item \textcolor{cyan}{*}: conta il numero di righe della tabella
    \item \textcolor{cyan}{DISTINCT ListaAttributi}: conta il numero di valori diversi degli attributi ListaAttributi
    \item \textcolor{cyan}{ALL ListaAttributi}: conta il numero di righe che hanno valori diversi da NULL negli attributi in ListaAttributi
    \begin{itemize}
        \item Comportamento di default
    \end{itemize}
\end{itemize}
  


\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/Q16.png}
\end{figure}


\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/Q17.png}
\end{figure}



\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/Q18.png}
\end{figure}

\subsubsection{ SUM, MAX, MIN, AVG}


  \begin{flushleft}
<\textcolor{cyan}{SUM} | \textcolor{cyan}{MAX} | \textcolor{cyan}{MIN} | \textcolor{cyan}{AVG} > ( [\textcolor{cyan}{DISTINC} | \textcolor{cyan}{ALL}] AttrExpr )    \end{flushleft}


  \begin{itemize}
    \item SUM: restituisce la somma dei valori posseduti dall’espressione
    \item MAX/MIN: restituisce il massimo/minimo dei valori posseduti dall’espressione
    \item AVG: restiusce la media dei valori
  \end{itemize}

Altre caratteristiche sono:
  \begin{itemize}
    \item SUM e AVG ammettono come argomento solo espressioni che rappresentano valori numerici o intervalli di tempo
    \item MAX e MIN ammettono come argomento solo espressioni su cui è definito un ordinamento (quindi anche stringhe)
    \item DISTINCT elimina i duplicati
    \item ALL trascura solo i valori nulli
  \end{itemize}

  \vspace{2em}
  \textbf{\large SUM}

  \begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/Q19.png}
  \end{figure}

    \textbf{\large MAX, MIN, AVG}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/Q20.png}
  \end{figure}


\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/Q21.png}
  \end{figure}

  \vario{ATTENZIONE}{

  \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]   
    \begin{flushleft}
        SELECT Cognone, Nome, MAX( I.Stipendio )\\
FROM Impiegato as I JOIN Diparmento as D\\
\hspace{2em}ON I.Dipart = D.Nome\\
WHERE D.Città = ‘Milano’\\
    \end{flushleft}
  \end{mdframed}
    \textcolor{black!60!red}{\textbf{La seguente interrogazione non è corretta!}}\par
Gli operatori aggregati non sono un meccanismo di selezione, ma solo
funzioni che restituiscono un valore quando sono applicate ad un insieme.\\
Cognome e Nome restituiscono un valore per ogni tupla selezionata, mentre
MAX() restituisce un valore unico per tutto l’insieme.\\
Per risolvere questa situazione serve il meccanismo di \textbf{raggruppamento} che
non vediamo}


  \subsection{Interrogazione con Raggruppamento}

  Gli operatori aggregate possono essere applicati separatamente a sottoinsiemi di righe.
  La clausola \textcolor{purple}{GROUP BY} permette di specificare come dividere le
tabelle in sottoinsiemi.
La clausola permette di specificare un insieme di attributi e l’interrogazione raggrupperà le righe che possiedono gli stessi valori per questo insieme di attributi


  \begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/Q22.png}
  \end{figure}


  \textbf{\large Esecuzione}




  \noindent
\begin{minipage}[c]{0.45\textwidth}
  \begin{enumerate}[series=passaggiSQL]
    \item Viene eseguita l’interrogazione come se la clausola GROUP BY non esistesse: 
    \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]      
    \begin{flushleft}
      \small
        SELECT Dipart, SUM(Stipendio)\\
        FROM Impiegato
    \end{flushleft}
  \end{mdframed}
  \end{enumerate}
    
\end{minipage}
 \hfill
\begin{minipage}[c]{0.45\textwidth}
    \centering
    % Inserisci qui il nome del tuo file immagine (es. schema.png)
    \includegraphics[width=\textwidth]{images/tabellina4.png}
\end{minipage}

  \noindent
\begin{minipage}[c]{0.45\textwidth}
  \begin{enumerate}[resume=passaggiSQL]
    \item La tabella ottenuta viene analizzata, dividendo le righe in insiemi caratterizzati dallo stesso valore degli attributi che compaiono nella clausola GROUP BY.
  \end{enumerate}
    
\end{minipage}
 \hfill
\begin{minipage}[c]{0.45\textwidth}
    \centering
    % Inserisci qui il nome del tuo file immagine (es. schema.png)
    \includegraphics[width=\textwidth]{images/tabellina5.png}
\end{minipage}




  \noindent
\begin{minipage}[c]{0.45\textwidth}
  \begin{enumerate}[resume=passaggiSQL]
    \item Dopo che le righe sono state raggruppate in sottoinsiemi, l’operatore aggregato viene applicato separatamente su ogni
sottoinsieme.
  \end{enumerate}
    
\end{minipage}
 \hfill
\begin{minipage}[c]{0.45\textwidth}
    \centering
    % Inserisci qui il nome del tuo file immagine (es. schema.png)
    \includegraphics[width=\textwidth]{images/tabellina6.png}
\end{minipage}


\vspace{2em}


In una interrogazione che fa uso della clausola GROUP BY può comparire come argomento della SELECT solo un sottoinsieme degli attributi usati nella
clausola GROUP BY oppure operatori di aggregazione.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/esempioGroupBu.png}
\end{figure}


 È possibile applicare delle selezioni ai gruppi generati tramite la clausola GROUP BY. La clausola \textcolor{purple}{HAVING} permette di specificare delle condizioni che devono essere soddisfatte dai gruppi generati dopo l’esecuzione del raggruppamento tramite GROUP BY.


 \vspace{2em}

 \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt] 
 \begin{flushleft}
         SELECT Dipart, SUM(Stipendio) as SommaStipendi\\
FROM Impiegato \\
GROUP BY Dipart\\
HAVING SUM(Stipendio) > 100\\

    \end{flushleft}
  \end{mdframed}



Dopo aver eseguito i raggruppamenti, vengono mantenuti solo i gruppi che soddisfano la condizione specificata nella clausola HAVING.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/esempio.png}
\end{figure}


\vario{OSSERVAZIONE}{ 
\begin{itemize}
  \item La clausola \textcolor{purple}{WHERE} applica delle selezioni su SINGOLE RIGHE, \textcolor{purple}{\textbf{prima}} dell’eventuale raggruppamento tramite GROUP BY.
\item La clausola \textcolor{purple}{HAVING} applica delle selezioni su GRUPPI, generati a \textcolor{purple}{\textbf{seguito}} dell’esecuzione del raggruppamento tramite GROUP BY
\end{itemize} .


\noindent
\begin{minipage}[c]{0.39\textwidth}
    % --- INIZIO RIQUADRO ---
    \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]
        \textbf{SQL}
        \begin{flushleft}
          \small 
          SELECT Dipart\\   
FROM Impiegato\\
WHERE Ufficio = 20\\
GROUP BY Dipart\\
HAVING AVG(Stipendio) > 25\\
        \end{flushleft}
    \end{mdframed}
    % --- FINE RIQUADRO ---
\end{minipage}
\hfill
\begin{minipage}[c]{0.60\textwidth} 
    \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]
        \begin{flushleft}
          \small 
          \begin{enumerate}
            \item WHERE→ seleziona le righe che
            verranno raggruppate 
            \item GROUP BY → forma i gruppi con le righe selezionate 
            \item HAVING → seleziona i gruppi che faranno parte del risultato
          \end{enumerate}
        \end{flushleft}
    \end{mdframed}
\end{minipage}

}





\subsection{Interrogazioni nidificate}


Una interrogazione nidificata è una interrogazione che contiene al suo interno altre interrogazioni.\par
La nidificazione può avvenire sia nella clausola \textcolor{cyan}{SELECT}, che nella
clausola \textcolor{cyan}{FROM}, che nella clausola \textcolor{cyan}{WHERE}.
Il caso tipico è la nidificazione nella clausola WHERE, dove un
predicato contiene un confronto tra un valore (espressione su una
singola riga) ed il risultato di un’interrogazione SQL.

\subsubsection{Interrogazioni nidificate: WHERE}

Se in un predicato si confronta un attributo o una espressione che
produce un valore singolo con il risultato di un’interrogazione, sorge il problema di disomogeneità dei termini del confronto. \par
I normali operatori di confronto (=, <>, <, >, <=, =>) possono essere estesi con le parole chiave \textcolor{cyan}{ANY} e \textcolor{cyan}{ALL}.

\begin{itemize}
  \item \textcolor{cyan}{ANY}: la riga soddisfa la condizione se risulta vero il confronto (con l’operatore specificato) tra il valore dell’attributo (o espressione) e \textcolor{cyan}{ALMENO} uno degli elementi restituiti dall’interrogazione. 
  \item \textcolor{cyan}{ALL}: la riga soddisfa la condizione se risulta vero il confronto (con l’operatore specificato) tra il valore dell’attributo (o espression) e \textcolor{cyan}{TUTTI} gli elementi restituiti dall’interrogazione.
\end{itemize}



  \noindent
\begin{minipage}[t]{0.49\textwidth}
   \begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/Q23.png}
  \end{figure}

\end{minipage}
\hfill % Questo comando inserisce uno spazio flessibile che spinge le due minipage ai lati opposti
\begin{minipage}[t]{0.49\textwidth}
      \begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/Q23_1.png}
  \end{figure}
  
\end{minipage}


   \begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/Q23_2.png}
   \end{figure}


   
   \begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/Q24.png}
   \end{figure}


   In particolare, scrivere: 
    \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]    
     \begin{flushleft}
        SELECT Nome\\
FROM Dipartimento\\
WHERE Nome <> ALL (SELECT Dipart\\
\hspace{4em}FROM Impiegato\\
\hspace{4em}WHERE Cognome = ‘Rossi’);

    \end{flushleft}
  \end{mdframed}


è equivalente a scrivere

    \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]    
     \begin{flushleft}
      SELECT Nome\\
FROM Dipartimento\\
\textcolor{cyan}{EXCEPT}\\
SELECT Dipart\\
FROM Impiegato\\
WHERE Cognome = ‘Rossi’
    \end{flushleft}
  \end{mdframed}




  
  Per il controllo di appartenenza o esclusione rispetto ad un insieme si possono uare anche gli operatori IN (= ANY) e NOT IN (<> ALL).

   \begin{mdframed}[backgroundcolor=gray!10, roundcorner=5pt]    
     \begin{flushleft}
    SELECT Nome\\
FROM Dipartimento\\
WHERE Nome NOT IN (SELECT Dipart\\
\hspace{4em}FROM Impiegato  \\
\hspace{4em}WHERE Cognome = ‘Rossi’);
    \end{flushleft}
  \end{mdframed}




  \vario{Attenzione!}{Per le interrogazioni nidificate viste fino ad ora, l’interrogazione
può essere eseguita una volta sola ed il risultato ottenuto può essere salvato in una tabella temporanea usata per eseguire
ciascun confronto con le righe dalla tabella esterna.\par 
Talvolta però l’interrogazione nidificata fa riferimento al contesto
dell’interrogazione che la racchiude attraverso l’uso di una
variabile (\textcolor{cyan}{passaggio di binding}).L’interrogazione nidificata dovrà essere \textcolor{cyan}{valutata separatamente per ciascuna riga} prodotta dalla valutazione della query esterna.}
 
  \subsubsection{Interrogazioni nidificate: Binding e EXISTS}

   L’operatore logico \textcolor{cyan}{EXISTS} riceve come parametro
un’interrogazione nidificata e restituisce il valore vero solo se
l’interrogazione fornisce un risultato non vuoto.\par 
Questo operatore può essere usato in modo significativo solo
quando si ha un passaggio di binding tra la query esterna e quella
interna




    \begin{figure}[H]
      \centering
      \includegraphics[width=\textwidth]{images/Q25.png} 
    \end{figure}



    
    \begin{figure}[H]
      \centering
      \includegraphics[width=\textwidth]{images/Q26.png} 
    \end{figure}


     \begin{figure}[H]
      \centering
      \includegraphics[width=\textwidth]{images/Q26alt.png} 
    \end{figure}


\subsubsection{Interrogazioni nidificate: SELECT}

Le interrogazioni nidificate possono essere specificate anche come elemento della clausola SELECT.
\begin{itemize}
  \item Spesso l’uso di query nidificate nella SELECT sostituiscono l’uso del JOIN.
\end{itemize}


 \begin{figure}[H]
      \centering
      \includegraphics[width=\textwidth]{images/Q27.png} 
    \end{figure}


    \subsubsection{Interrogazioni nidificate: FROM}

    Le interrogazioni nidificate possono essere utilizzate nella
clausola FROM come sorgente dati su cui eseguire
l’interrogazione.\par
È possibile creare una catena di interrogazioni, dove ciascuna
interrogazione utilizza il risultato della precedente come una
tabella di base.


 \begin{figure}[H]
      \centering
      \includegraphics[width=\textwidth]{images/Q28.png} 
    \end{figure}  

    





    \chapter{Laboratorio}
PostgrSQL è un Relational Data Base Management System (RDBMS o DBMS) con alcune funzionalità orientate agli oggetti. L’interazione tra un utente (programmatore/utente finale) e le basi di dati gestite da PostgreSQL avviene secondo il modello client-server.

    \begin{figure}[H]
      \centering
      \includegraphics[width= \textwidth]{images/ClientServer.png}
    \end{figure}

    \section{Creazione di una tabella}
Struttura base
\begin{lstlisting}
CREATE TABLE tabella (
  attributo dominio [ valoreDefault ] { vincolo }
  {, attributo dominio [ valoreDefault ] { vincolo } }
  {, vincoloTabella }
]);
\end{lstlisting}

Costrutti inseriti: 
\begin{itemize}
  \item {[}{]} indicano parti opzionali / facoltative (il termine può non comparire o comparire una sola volta) Indica che quell'elemento può apparire zero o una volta. Sta al programmatore decidere se inserirlo o meno a seconda di ciò che si vuole ottenere)
\item {[} valoreDefault {]}: Significa che quando definisco l’attributo posso decidere se attribuirgli un valore predefinito
\item \{ \} Tutto ciò che è racchiuso tra parentesi graffe rappresenta un elemento che può essere ripetuto zero, una o più volte.
\item \{ vincolo \}: Significa che a una singola colonna puoi applicare nessun vincolo, un solo vincolo, oppure una serie di vincoli uno dopo l'altro (es. NOT NULL, UNIQUE)
\end{itemize}


\textbf{\large Ecco un esempio}
\begin{lstlisting}
CREATE TABLE Impiegato (
  Matricola CHARACTER(6) PRIMARY KEY, 
  Nome VARCHAR(20) NOT NULL, 
  Cognome VARCHAR(20) NOT NULL,
  Dipart VARCHAR(15), 
  Stipendio NUMERIC(9) DEFAULT 0,
  FOREIGN KEY(Dipart) 
    REFERENCES Dipartimento(NomeDip),     /*vincolo di tabella*/
  UNIQUE (Cognome,Nome)                   /*vincolo di tabella*/
)
\end{lstlisting}


\subsection{Identificatori}
 Sono stringhe che iniziano con una lettera (UTF-8) o un underscore (\_) e che possono contenere anche cifre (0-9). Gli identificatori SQL \textbf{NON} sono sensibili al minuscolo/maiuscolo:
 \begin{itemize}
  \item idPersona e idpersona rappresentano un solo identificatore.
 \end{itemize}

\subsection{Domini}
I domini elementari (predefiniti) sono:
\begin{itemize}
  \item \textbf{caratteri}: singoli caratteri o stringhe anche di lunghezza variabile
  \item \textbf{bit}: bit singoli (booleani) o stringhe di bit
  \item \textbf{numerici esatti e approssimati}
  \item \textbf{istanti temporali}
  \item \textbf{intervalli temporali}
\end{itemize}
Oppure si possono avere domini definiti dall'utente

\subsubsection{Tipo carattere}
Permette di rappresentare singoli caratteri oppure stringhe.
Un carattere o stringa di caratteri sono rappresentati tra apici (’):
’a’, ’Questa è una stringa’.\par
La lunghezza delle stringhe può essere fissa o variabile: 
\textcolor{cyan}{CHARACTER [VARYING][(lunghezza)]}
\begin{itemize}
  \item \textbf{CHARACTER}: carattere singolo
  \item \textbf{CHARACTER(20)}: stringa di \textbf{lunghezza fissa}. Se si assegna una stringa di lunghezza inferiore a 20, la stringa viene riempita di spazi fino a rendere la stringa lunga 20.
  \item \textbf{CHARACTER VARYING(20)}: stringa di \textbf{lunghezza variabile} (max 20).
  \item \textbf{TEXT}: \underline{stringa di lunghezza variabile senza limite fissato}. Questa è un’estensione PostgreSQL
\end{itemize}

\subsubsection{Tipo boolean}
Permette di rappresentare i valori booleani TRUE/FALSE, rappresentati anche 
come t/f, 1/0, yes/NO, più lo stato unknown (valore nullo), rappresentato 
come NULL

\subsubsection{Tipo numerici esatti}
Permettono di rappresentare valori esatti: interi o con una parte decimale di 
lunghezza prefissata
\begin{itemize}
  \item \textbf{Valori interi}:
\begin{itemize}
  \item SMALLINT: valori interi (2 bytes: da -32.768 (215) a +32.767 (215-1)).
  \item INTEGER: valori interi (4 bytes: da -2147483648 (231) to +2147483647 (231-1) ).
\end{itemize}
\item \textbf{Valori decimali}:
\begin{itemize}
  \item NUMERIC [(precisione [, scala])]: precisione è il numero totale di cifre  significative (cioè di cifre a sinistra e a destra della virgola), scala è il numero di cifre dopo la virgola (vuoto = 0).
\item DECIMAL [(precisione [, scala])]: equivalente al precedente.
\end{itemize}
\end{itemize}

\esempio{}{NUMERIC(5,2) permette di rappresentare valori come 100.01, 100.999 
(arrotondato a 101.00), ma non valori come 1000.01}

\subsubsection{Tipo numerici approssimati}
Permettono di rappresentare valori in virgola mobile:
\begin{itemize}
  \item \textbf{REAL}: una precisione di 6 cifre decimali.
  \item \textbf{DOUBLE PRECISION}: una precisione di 15 cifre decimali.
\end{itemize}
Ciascun numero è rappresentato da una coppia di valori: la mantissa e 
l’esponente.
\begin{itemize}
  \item La mantissa è un numero frazionario, l’esponente è un numero intero
  \item Il valore si ottiene moltiplicando la mantissa per la potenza di 10 con grado pari all’esponente
\end{itemize}
Il numero 0.17E16 corrisponde a 1,7 x 1015           
Il numero -0.4E-6 corrisponde a -4 x 10-7

\subsubsection{Istanti temporali}
Permettono di rappresentare istanti di tempo.
\begin{itemize}
  \item \textbf{DATE}: istanti del tipo (anno, mese, giorno).
  \begin{itemize}
    \item Un valore DATE si rappresenta tra apici (’) e lo standard ISO prescrive il formato \texttt{YYYY-MM-DD}.
  \end{itemize}
  \item \textbf{TIME [(precisione)][WITH TIME ZONE]}: istanti del tipo (hour, minute, second)
  \begin{itemize}
    \item precisione: numero cifre decimali per rappresentare frazioni del secondo;
    \item WITH TIME ZONE: permette di specificare anche [+-]hour:minute, che rappresentano la differenza con l’ora di Greenwich, o nomeFusoOrario.
  \end{itemize}
  \item \textbf{TIMESTAMP [(precisione)][WITH TIME ZONE]}: date + time.
\end{itemize}

\subsubsection{Intervalli temporali}
INTERVAL PrimaUnitàDiTempo[TO UltimaUnitàDiTempo]
PrimaUnitàDiTempoe UltimaUnitàDiTempodefiniscono le unità di misura che 
devono essere utilizzate, dalla più precisa alla meno precisa.
L’insieme delle unità di misura è diviso in due insiemi distinti:
\begin{itemize}
  \item Year-Month
  \item Day-Hour–Minute-Second
\end{itemize}

\subsubsection{Domini definiti dall'utente}

\begin{lstlisting}
  CREATE DOMAIN nome AS tipoBase [valoreDefault][vincolo]
\end{lstlisting}


\textcolor{purple}{\textbf{CREATE DOMAIN}}: definisce un dominio, utilizzabile in definizioni di relazioni, anche con 
vincoli e valori di default
Il dominio può essere definito a partire da un dominio elementare oppure da un altro 
dominio definito dall‘utente
\begin{lstlisting}
CREATE DOMAIN giorniSettimana AS CHARACTER (3)
CHECK (VALUE IN ('LUN', 'MAR', 'MER', 'GIO', 'VEN', 'SAB', 'DOM'));
\end{lstlisting}

\begin{lstlisting}
CREATE DOMAIN Voto AS SMALLINT DEFAULT NULL
CHECK ( value >=18 AND value <= 30 )
\end{lstlisting}




\subsection{Vincoli intrarelazionali}

\begin{itemize}
  \item \textbf{NOT NULL}: il valore nullo non è ammesso per l'attributo, quindi il valore deve sempre essere specificato, o si assegna un valore di default
  \item \textbf{UNIQUE}: definisce (super)chiavi
  \begin{lstlisting}
Nome VARCHAR(20),
Cognome VARCHAR(20),
UNIQUE(Nome,Cognome)

/* impone che non ci siano due righe che abbiano uguale sia il nome che il cognome*/
  \end{lstlisting}

  \begin{lstlisting}
Nome VARCHAR(20) UNIQUE,
Cognome VARCHAR(20) UNIQUE

/* impone che non ci siano due righe che abbiano lo stesso nome o lo stesso cognome*/ 
  \end{lstlisting}
  \item \textbf{PRIMARY KEY}: chiave primaria (una sola, implica NOT NULL) 
  \begin{lstlisting}
matricola CHAR (6) PRIMARY KEY;
/* se unico componente della chiave primaria*/
  \end{lstlisting}

  \begin{lstlisting}
nome VARCHAR(20),
cognome VARCHAR(20),
PRIMARY KEY(nome, cognome)

/* Se la chiave primaria ha piu attributi */
  \end{lstlisting}
  \item \textbf{CHECK}: specifica un vincolo generico che deve soddisfare le tuple della tabella. Viene soddisfatto se la sua espressione è vera o nulla.
  
\begin{lstlisting}
CREATE TABLE Impiegato (
matricola CHAR(6) PRIMARY KEY,
nome VARCHAR(20) NOT NULL,
cognome VARCHAR(20) NOT NULL,
qualifica VARCHAR(20),
stipendio NUMERIC(8,2) DEFAULT 500.00 NOT NULL
CHECK( stipendio >= 0.0 ),    /* check di attributo */
UNIQUE( cognome, nome ),
CHECK( nome <> cognome )      /* check di tabella */
)
  \end{lstlisting}

\end{itemize}

\subsection{Vincoli interrelazionali}
Un vincolo di integrità referenziale (= interrelazionale) crea un legame tra i 
valori di un attributo (o di un insieme di attributi) A della tabella corrente (detta interna/slave) e i valori di un attributo (o di un insieme di attributi) B di un’altra tabella (detta esterna/master):
\begin{itemize}
  \item Impone che, in ogni tupla della tabella interna, il valore di A, se diverso dal valore nullo, sia presente tra i valori di B nella tabella esterna.
  \item  L’attributo B della tabella esterna deve essere soggetto a un vincolo 
UNIQUE (o PRIMARY KEY).
\end{itemize}
\vario{Nota:}{L’attributo (o insieme di attributi) B può anche non essere la 
chiave primaria, ma deve essere identificante per le tuple della tabella 
esterna}

\textbf{REFERENCES e FOREIGN KEY permettono di definire vincoli di integrità referenziale}

Per singoli attributi, basta utilizzare il costrutto \textcolor{purple}{\textbf{REFERENCES}}
\begin{lstlisting}
CREATE TABLE Impiegato (
  Matricola CHARACTER(6) primary key,
  Nome CHARACTER VARYING(20)  not null,
  Cognome CHARACTER VARYING(20)  not null,
  Dipart CHARACTER VARYING(15) 
  REFERENCES Dipartimento(NomeDip),   
  /* Attributo chiave (singolo) di tabella esterna*/
  Ufficio NUMERIC(3),
  Stipendio  NUMERIC(9) default 0,
  UNIQUE (Cognome, Nome)
)
\end{lstlisting}

Per attributi multipli, si usa il costrutto \textcolor{purple}{\textbf{FOREIGN KEY}} alla fine della definizione degli attributi, seguito da \textcolor{purple}{\textbf{REFERENCES}}
\begin{lstlisting}
CREATE TABLE Impiegato (
  Matricola CHARACTER(6) primary key,
  Nome CHARACTER VARYING(20)  not null,
  Cognome CHARACTER VARYING(20)  not null,
  Dipart CHARACTER VARYING(15),
  REFERENCES Dipartimento(NomeDip),   
  Ufficio NUMERIC(3),
  Stipendio  NUMERIC(9) default 0,
  FOREIGN KEY(Nome, Cognome)
    REFERENCES Anagrafica (Nome, Cognome) 
    /* Tabella esterna con attributi chiave*/
)
\end{lstlisting}

Vediamo un esempio più concreto:

\begin{figure}[H]
  \centering
  \includegraphics[width= \textwidth]{images/esempioReferences.png}
\end{figure}

\begin{lstlisting}
CREATE TABLE Infrazione(
  Codice CHAR(6) PRIMARY KEY, 
  Data DATE NOT NULL,  
  Vigile INTEGER NOT NULL
  REFERENCES Vigile(Matricola),
  Provincia CHAR(2),  
  Numero CHAR(6) ,
  FOREIGN KEY(Provincia, Numero)
    REFERENCES Auto(Provincia, Numero)
)

\end{lstlisting}

\section{Modifica degli schemi}
\begin{itemize}
  \item ALTER: effettua modifiche (a domini, tabelle...), ad esempio aggiungere una colonna a una relazione
  \item DROP DOMAIN: permette di rimuovere i domini
  \item DROP TABLE: cancella una tabella
\end{itemize}

\subsection{ALTER: modifiche alla struttura di una tabella}
Il costrutto \textbf{Alter} permette di:

Aggiungere un nuovo attributo (ADD COLUMN):
\begin{lstlisting}
ALTER TABLE tabella ADD COLUMN attributo tipo;
ALTER TABLE impiegato ADD COLUMN stipendio NUMERIC(8,2);
\end{lstlisting}

Rimuovere un attributo (DROP COLUMN):
\begin{lstlisting}
ALTER TABLE tabella DROP COLUMN attributo;
ALTER TABLE impiegato DROP COLUMN stipendio;
\end{lstlisting}
Modificare valore di default di un attributo (ALTER COLUMN):
\begin{lstlisting}
ALTER TABLE tabella ALTER COLUMN attributo { SET DEFAULT valore | 
DROP DEFAULT };
ALTER TABLE impiegato ALTER COLUMN stipendio SET DEFAULT 1000.00;
\end{lstlisting}

\section{Cancellazione dati in una tabella}
Le tuple di una tabella vengono cancellate con il comando \textcolor{purple}{\textbf{DELETE}}.
condizione è una espressione booleana che seleziona quali tuple cancellare. 
\begin{lstlisting}
  DELETE FROM tabella [ WHERE condizione ];
\end{lstlisting}

\vario{}{
  \textbf{Se WHERE non è presente, tutte le tuple saranno cancellate.}
}
\begin{lstlisting}
  DELETE FROM impiegato WHERE matricola = 'A001 ';
\end{lstlisting}

Una tabella viene cancellata con il comando \textcolor{purple}{\textbf{DROP TABLE}}:
\begin{lstlisting}
  DROP TABLE tabella;
\end{lstlisting}




\section{Inserimento e aggiornamento di tuple}

\textbf{\large Inserimento di valori all'interno di una tupla}

\begin{lstlisting}
INSERT INTO Tabella [ ( Attributi ) ] 
VALUES( Valori ) 
\end{lstlisting}
oppure
\begin{lstlisting}
  INSERT INTO Tabella [ ( Attributi )]
(SELECT ...)
\end{lstlisting}

\textbf{ Esempio inserimento}
\begin{lstlisting}
INSERT INTO Museo (nome, citta, indirizzo, numeroTelefono, giornoChiusura, prezzo)
VALUES('Arena', 'Verona', 'piazza Bra', '045 8003204', 'MARTEDI', 20) 

\end{lstlisting}


\textbf{\large Aggiornamento di valori all'interno di una tupla}

\begin{lstlisting}
UPDATE NomeTabella
  SET Attributo = < 
  Espressione |
  SELECT ... | 
  NULL | 
  DEFAULT >
  [ WHERE Condizione ]
\end{lstlisting}

\textbf{Esempio}
\begin{lstlisting}
UPDATE Museo SET sitoInternet = 'www.ciao.it' WHERE citta ='Verona'  
\end{lstlisting}


\section{Selezione}

\textbf{\large Struttura base}

\begin{lstlisting}
SELECT [ DISTINCT ]
[ * | expression [[ AS] output_name ] [, 
...] ]
FROM from_item [, ...] 
[ WHERE condition ]
[ GROUP BY grouping_element [, ...] ]
[ HAVING condition [, ...] ]
[ { UNION | INTERSECT | EXCEPT } [ DISTINCT
] other_select ]
[ ORDER BY expression [ ASC | DESC | USING 
operator ]]

\end{lstlisting}


\subsection{Clausola SELECT}
\begin{lstlisting}
SELECT [ DISTINCT ]
[{* | expression [[ AS] output_name ]} [, ...]]
\end{lstlisting}
\begin{description}
  \item [\textbf{*}] è un’abbreviazione per indicare tutti gli attributi delle tabelle.
  \item [\textbf{expression}] è un’espressione che coinvolge gli attributi della tabella (può anche essere semplicemente il nome di un attributo).
  \item [\textbf{output\_name}] è il nome assegnato all’attributo che conterrà il risultato della valutazione dell’espressione expression nella relazione risultato.
  \item [\textbf{DISTINCT}:] se presente richiede l’eliminazione delle tuple duplicate.

\end{description}


L’esecuzione di una \textbf{SELECT} produce una relazione che:
\begin{itemize}
  \item ha come schema tutti gli attributi elencati nella clausola \textbf{SELECT};
  \item ha come contenuto tutte le tuple ottenute proiettando sugli attributi 
indicati nella \textbf{SELECT} provenienti dalle tabelle del \textbf{FROM} (prodotto 
cartesiano), limitate a quelle che soddisfano le eventuali condizioni 
specificate nelle clausole \textbf{WHERE}, \textbf{GROUP BY} e \textbf{HAVING}
\end{itemize}


\subsubsection{Target List}

\begin{minipage}[t]{0.39\textwidth}
   \begin{lstlisting}
SELECT *
FROM Tabella
   \end{lstlisting}
\end{minipage}
\hfill % Questo comando inserisce uno spazio flessibile che spinge le due minipage ai lati opposti
\begin{minipage}[t]{0.50\textwidth}
  \vspace{1em}
   Selezione di tutti gli attributi 
delle tabelle indicate nella 
clausola FROM
\end{minipage}

\begin{minipage}[t]{0.39\textwidth}
   \begin{lstlisting}
SELECT Attributo1, Attributo2
FROM Tabella
   \end{lstlisting}
\end{minipage}
\hfill % Questo comando inserisce uno spazio flessibile che spinge le due minipage ai lati opposti
\begin{minipage}[t]{0.50\textwidth}
  \vspace{1em}
   Selezione di tutti gli attributi 
delle tabelle indicate nella 
clausola FROM
\end{minipage}


\textbf{Esempio}



\begin{minipage}[t]{0.39\textwidth}
 \begin{lstlisting}
SELECT Nome, Reddito
FROM Persone
WHERE Eta < 30
\end{lstlisting}
\end{minipage}
\hfill % Questo comando inserisce uno spazio flessibile che spinge le due minipage ai lati opposti
\begin{minipage}[t]{0.50\textwidth}
\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{images/nomeReddito.png}
  \caption{selezionati solo le tuple dove è rispettata la condizione}
\end{figure}
\end{minipage}


\subsection{Clausola FROM}
\begin{lstlisting}
  FROM from_item [, ...]
\end{lstlisting}

\begin{enumerate}
  \item  table\_name [[AS] alias [(column\_alias [, ...])]]
  \begin{itemize}
    \item Se sono presenti due o più tabelle, si esegue il prodotto cartesiano tra tutte le  tabelle. Se ci sono attributi con lo stesso nome su tabelle diverse, nel SELECT 
questi attributi sono identificati anteponendo il nome della tabella e un ’.’: 
nomeTabella.nomeAttributo
  \end{itemize}

  \item  (other\_select) [AS] nomeRisultato [(column\_alias [,...])
  \begin{itemize}
    \item  è una SELECT innestata. Il risultato della SELECT interna è lo schema con nome 
nomeRisultato su cui fare la SELECT corrente.
  \end{itemize}

  \item  from\_item [NATURAL] join\_type from\_item [ON condition]
  \begin{itemize}
    \item  è la clausola JOIN.
    \item Metodo più sofisticato di 1) che permette di selezionare un sottoinsieme del 
prodotto cartesiano di due o più tabelle. join\_type specifica il tipo di join. I tipi 
principali sono riassunti nelle slide seguenti
  \end{itemize}
\end{enumerate}


\subsubsection{Uso di alias}

\begin{lstlisting}
SELECT P.Reddito, R.Telefono
FROM Reparti as R, Persona ad P
WHERE ... ;
\end{lstlisting}

Utile quando è necessario specificare la provenienza dell’attributo, 
perchè nella base di dati sono presenti più attributi con lo stesso nome

\subsubsection{Target List}
\begin{lstlisting}
SELECT P.Nome as NPersona, P.Reddito as RPersona
FROM Persone as P
WHERE P.Eta < 30
\end{lstlisting}




\subsection{Clausola WHERE}
\begin{lstlisting}
  WHERE condition
\end{lstlisting}

\textbf{\textcolor{black!50!red}{condition}} è un’espressione booleana combinando condizioni semplici $C_{i}$ con gli 
operatori AND, OR, NOT.
\begin{enumerate}
  \item  $C_{i}$ := expr [ $\theta$ \{expr | const\}]
dove
\begin{itemize}
  \item expr := espressione che contiene riferimenti agli attributi delle tabelle che compaiono 
nella clausola FROM.
\item $\theta \in$ \{=,<>,<,>,<=,>=\}
\item const := valore dei domini di base.
\end{itemize}
\item Una riga soddisfa \textcolor{black!50!red}{condition} se l’espressione è vera quando valutata con i valori 
della riga.
\item Una riga che non soddisfa condition è scartata dal risultato finale
\end{enumerate}





\begin{minipage}[t]{0.39\textwidth}
 \begin{lstlisting}
SELECT Nome, Reddito
FROM Persone
WHERE Eta < 30;
\end{lstlisting}
\end{minipage}
\hfill % Questo comando inserisce uno spazio flessibile che spinge le due minipage ai lati opposti
\begin{minipage}[t]{0.50\textwidth}
SELECT Nome, Reddito
FROM Persone
WHERE Età < 30 OR Reddito > 50;
\end{minipage}




\subsubsection{Operatore LIKE}
\begin{lstlisting}
WHERE attributo [NOT] LIKE 'pattern';
\end{lstlisting}

Nella clausola WHERE può apparire l’operatore LIKE per il confronto di stringhe. 
LIKE è un operatore di pattern matching. I pattern si costruiscono con i 
caratteri speciali \_ e \%:
\begin{itemize}
  \item \_ = 1 carattere qualsiasi.
  \item \% = 0 o più caratteri qualsiasi.
\end{itemize}


\begin{minipage}[t]{0.39\textwidth}
  \vspace{2em}
  \begin{lstlisting}[aboveskip=0pt] % <-- Azzera lo spazio vuoto sopra il codice
SELECT Nome,Reddito
FROM Persona
WHERE Nome LIKE 'A\%a'
  \end{lstlisting}
\end{minipage}
\hfill 
\begin{minipage}[t]{0.50\textwidth}
  \vspace{0pt}
  % Rimosso totalmente \begin{figure}[H] e \end{figure}
  \includegraphics[width=0.8\textwidth]{images/nomeReddito2.png}
\end{minipage}  


\subsubsection{Operatore SIMILAR TO}


L’operatore \textbf{\textcolor{purple}{SIMILAR TO}} è un \textbf{\textcolor{purple}{LIKE}} più espressivo che accetta, come pattern, 
un sottoinsieme delle espressioni regolari POSIX (versione SQL). 
Esempi di componenti di espressioni regolari:
\begin{itemize}
  \item \_ = 1 carattere qualsiasi. 
  \item \% = 0 o più caratteri qualsiasi.
  \item * = ripetizione del precedente match 0 o più volte.
  \item + = ripetizione del precedente match UNA o più volte.
  \item \{n,m\} = ripetizione del precedente match almeno n e non più di m volte.
  \item {[} ... {]} = ... è un elenco di caratteri ammissibili
\end{itemize}


\begin{minipage}[t]{0.50\textwidth}
  \vspace{2em}
  \begin{lstlisting} 
SELECT *
FROM Persone
Per trovare nomi che
iniziano con A, F o O
WHERE Nome SIMILAR TO '[AFO]'
  \end{lstlisting}
\end{minipage}
\hfill 
\begin{minipage}[t]{0.45\textwidth}
  \vspace{3em}
Per trovare nomi che inziano per A,F o O
\end{minipage}  


\subsubsection{Operatore BETWEEN}

\begin{lstlisting}
WHERE attributo [NOT] BETWEEN valore_iniziale AND valore_finale;
\end{lstlisting}

Nella clausola \textcolor{purple}{\textbf{WHERE}} può apparire l’operatore \textcolor{purple}{\textbf{BETWEEN}} per testare l’appartenenza 
di un valore ad un intervallo (non limitato ai valori numerici, usato anche con date e 
stringhe)


\begin{minipage}[t]{0.40\textwidth}
  \begin{lstlisting} 
SELECT *
FROM Persone
WHERE Eta BETWEEN 20 AND 40
  \end{lstlisting}
\end{minipage}
\hfill 
\begin{minipage}[t]{0.45\textwidth}
  \includegraphics[width=0.8\textwidth, valign=t]{images/nomeEtaReddito.png}
\end{minipage}


\subsubsection{Operatore IN}
\begin{lstlisting}
  WHERE attributo [NOT] IN ( valore [, ...] );

\end{lstlisting}
Nella clausola WHERE può apparire l’operatore [\textbf{\textcolor{purple}{NOT}}] \textbf{\textcolor{purple}{IN}} per testare 
l’appartenenza di un valore ad un insieme.
\begin{lstlisting}
SELECT *
FROM Persone
WHERE Nome NOT IN ('Andrea', 'Franco')
\end{lstlisting}

\subsubsection{Operatore IS NULL}
\begin{lstlisting}
WHERE attributo IS [ NOT ] NULL;
\end{lstlisting}
Nella clausola WHERE può apparire l’operatore \textbf{\textcolor{purple}{IS}} [\textbf{\textcolor{purple}{NOT}}] \textbf{\textcolor{purple}{NULL}} per testare se un 
valore è \textbf{\textcolor{purple}{NOT KNOWN (NULL)}} o no.
Il valore NULL non rappresenta lo zero o una stringa vuota, ma l'assenza di dati 
o un valore sconosciuto.
A partire dalle versioni SQL-2 è stata adottata una logica a tre valori (true, 
false, unknown) e i valori nulli non sono considerati né veri, né falsi.\par
\begin{center}
NON SI PUÒ usare ’=’ o ’<>’ con il valore NULL!  
\end{center}

\begin{lstlisting}
SELECT *
FROM Persone
WHERE Reddito IS NOT NULL
\end{lstlisting}

\subsubsection{Operatore ORDER BY}
\begin{lstlisting}
  ORDER BY attributo [{ ASC | DESC }] [, ... ];
\end{lstlisting}
La clausola ORDER BY ordina le tuple del risultato in ordine rispetto agli attributi 
specificati,
ASCendente è lo standard.
\begin{lstlisting}
SELECT Nome
FROM Persone
ORDER BY Nome DESC

\end{lstlisting}

\subsubsection{Operatori di aggregazione}




  \end{document}